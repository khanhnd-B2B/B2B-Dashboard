name: Auto Update Data

on:
  schedule:
    # Chạy mỗi 4 tiếng (theo giờ UTC, GitHub Actions dùng giờ UTC)
    - cron: '0 */4 * * *'
  workflow_dispatch: # Cho phép bấm chạy thủ công trên GitHub

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas

      - name: Run fetch_data.py
        run: python fetch_data.py

      - name: Commit and push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add master_data.csv
          git commit -m "Auto-update data from Google Sheets" || echo "No changes to commit"
          git push
